spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  image:
    savePath: /var/lib/storage/images/
  jpa:
    hibernate:
      ddl-auto: create